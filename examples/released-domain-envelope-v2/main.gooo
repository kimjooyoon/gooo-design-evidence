package releaseddomainenvelope
namespace designevidencereleasedenvelope

entity CurrentCoreRelease id "design://released-domain-envelope/entity/current-core-release"
entity LocalProductRelease id "design://released-domain-envelope/entity/local-product-release"
entity DesignProductRelease id "design://released-domain-envelope/entity/design-product-release"
entity InfraProductRelease id "design://released-domain-envelope/entity/infra-product-release"
entity DomainRelation id "design://released-domain-envelope/entity/domain-relation"
entity ReleasedEvidenceAnchor id "design://released-domain-envelope/entity/released-evidence-anchor"
entity ClaimResolution id "design://released-domain-envelope/entity/claim-resolution"
entity EightFileEnvelope id "design://released-domain-envelope/entity/eight-file-envelope"
entity SixUnknownCoordinates id "design://released-domain-envelope/entity/six-unknown-coordinates"
entity DeterministicReplay id "design://released-domain-envelope/entity/deterministic-replay"
entity InvalidEnvelopeRefutation id "design://released-domain-envelope/entity/invalid-envelope-refutation"
entity ReadOnlyAuthority id "design://released-domain-envelope/entity/read-only-authority"
entity AdoptionCandidateDecision id "design://released-domain-envelope/entity/adoption-candidate-decision"

activity ObserveCurrentCoreRelease(CurrentCoreRelease) -> AdoptionCandidateDecision
activity ObserveLocalProductRelease(LocalProductRelease) -> AdoptionCandidateDecision
activity ObserveDesignProductRelease(DesignProductRelease) -> AdoptionCandidateDecision
activity ObserveInfraProductRelease(InfraProductRelease) -> AdoptionCandidateDecision
activity SeparateDomainRelation(DomainRelation) -> AdoptionCandidateDecision
activity AnchorReleasedEvidence(ReleasedEvidenceAnchor) -> AdoptionCandidateDecision
activity SeparateClaimResolution(ClaimResolution) -> AdoptionCandidateDecision
activity PublishEightFileEnvelope(EightFileEnvelope) -> AdoptionCandidateDecision
activity PreserveSixUnknownCoordinates(SixUnknownCoordinates) -> AdoptionCandidateDecision
activity VerifyMachineReadableReplay(DeterministicReplay) -> AdoptionCandidateDecision
activity RefuteInvalidEnvelope(InvalidEnvelopeRefutation) -> AdoptionCandidateDecision
activity PreserveReadOnlyAuthority(ReadOnlyAuthority) -> AdoptionCandidateDecision
