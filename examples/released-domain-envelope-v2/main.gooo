package releaseddomainenvelope
namespace designevidencereleasedenvelope

entity CurrentCoreRelease id "design://released-domain-envelope/entity/current-core-release"
entity SpecificationRelease id "design://released-domain-envelope/entity/specification-release"
entity DesignRelease id "design://released-domain-envelope/entity/design-release"
entity MetaActivityAuthority id "design://released-domain-envelope/entity/meta-activity-authority"
entity ClaimDispositionSource id "design://released-domain-envelope/entity/claim-disposition-source"
entity ProductProjection id "design://released-domain-envelope/entity/product-projection"
entity EightFileEnvelope id "design://released-domain-envelope/entity/eight-file-envelope"
entity ReadOnlyConformance id "design://released-domain-envelope/entity/read-only-conformance"
entity UnknownCausality id "design://released-domain-envelope/entity/unknown-causality"
entity DeterministicReplay id "design://released-domain-envelope/entity/deterministic-replay"
entity RefutedCounterexamples id "design://released-domain-envelope/entity/refuted-counterexamples"
entity AuthorityBoundary id "design://released-domain-envelope/entity/authority-boundary"
entity AdoptionCandidateDecision id "design://released-domain-envelope/entity/adoption-candidate-decision"

activity ObserveCurrentCoreRelease(CurrentCoreRelease) -> AdoptionCandidateDecision
activity ObserveSpecificationRelease(SpecificationRelease) -> AdoptionCandidateDecision
activity ObserveDesignRelease(DesignRelease) -> AdoptionCandidateDecision
activity BindMetaActivityAuthority(MetaActivityAuthority) -> AdoptionCandidateDecision
activity ObserveClaimDispositionSource(ClaimDispositionSource) -> AdoptionCandidateDecision
activity GenerateProductProjection(ProductProjection) -> AdoptionCandidateDecision
activity ObserveEightFileEnvelope(EightFileEnvelope) -> AdoptionCandidateDecision
activity VerifyReadOnlyConformance(ReadOnlyConformance) -> AdoptionCandidateDecision
activity PreserveUnknownCausality(UnknownCausality) -> AdoptionCandidateDecision
activity VerifyDeterministicReplay(DeterministicReplay) -> AdoptionCandidateDecision
activity RefuteCounterexamples(RefutedCounterexamples) -> AdoptionCandidateDecision
activity PreserveAuthorityBoundary(AuthorityBoundary) -> AdoptionCandidateDecision
